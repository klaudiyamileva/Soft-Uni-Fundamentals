function requiredReading(allPages, pagesPerHour, days) {
    let allHours = allPages / pagesPerHour;
    let hoursPerDay = allHours / days;

    console.log(ho)

}
requiredReading(212,20,2);
requiredReading(432,15,4);